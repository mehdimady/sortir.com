{% extends 'base.html.twig' %}

{% block title %}
{{title}}
{% endblock %}

{% block body %}
    {{form_start(sortieForm)}}
    {{ form_widget(sortieForm) }}
    <button>Enregistrer</button>
    {{form_end(sortieForm)}}
{% endblock %}
