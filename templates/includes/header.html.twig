<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
            <a class="navbar-brand f-lunatic anim text-light fs-3"  href="{{path('app_home')}}">Sortir.com</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
             <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                {% if app.user  %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_profil',{'id':app.user.id}) }}" title="Mon Profil">Mon Profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}" title="Logout">Se déconnecter</a>
                    </li>
                    {% if app.user.administrateur %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{path('ville_toutes')}}">Villes</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{path('campus_tous')}}">Campus</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link f-lunatic active text-light" aria-current="page" href="{{path('admin_tous')}}">Admin</a>
                        </li>
                    {% endif %}
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_register') }}" title="Register">Créer un compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_login') }}" title="Login">Se Connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>